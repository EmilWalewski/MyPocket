import { ComponentsUnwrapperDirective } from './components-unwrapper.directive';

describe('ComponentsUnwrapperDirective', () => {
  it('should create an instance', () => {
    const directive = new ComponentsUnwrapperDirective();
    expect(directive).toBeTruthy();
  });
});
